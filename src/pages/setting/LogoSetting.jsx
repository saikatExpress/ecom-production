import useTitle from '../../hooks/useTitle';

const LogoSetting = () => {
    // Hook
    useTitle("Logo Setting");

    return (
        <>
            
        </>
    );
};

export default LogoSetting;