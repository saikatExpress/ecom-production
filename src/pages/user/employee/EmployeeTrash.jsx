import useTitle from '../../../hooks/useTitle';

const EmployeeTrash = () => {
    // Hook
    useTitle("Employee Trash List");

    return (
        <>
            
        </>
    );
};

export default EmployeeTrash;