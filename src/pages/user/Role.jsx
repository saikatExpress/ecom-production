import useTitle from "../../hooks/useTitle";

const Role = () => {
    // Hook
    useTitle("Role with permissions list");

    return (
        <>
            
        </>
    );
};

export default Role;