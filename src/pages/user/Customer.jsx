import useTitle from '../../hooks/useTitle';

const Customer = () => {
    // Hook
    useTitle("Customer List");
    
    return (
        <>
            <h1>Customer List</h1>
        </>
    );
};

export default Customer;