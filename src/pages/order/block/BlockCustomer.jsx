import useTitle from '../../../hooks/useTitle';

const BlockCustomer = () => {
    // Hook
    useTitle("Block Customer List");

    return (
        <>
            
        </>
    );
};

export default BlockCustomer;