import { useNavigate } from 'react-router-dom';
import useTitle from '../../hooks/useTitle';

const CourierIntegrate = () => {
    // Hook
    useTitle("Courier Implementation");

    // Variable
    const navigate = useNavigate();

    return (
        <div>
            
        </div>
    );
};

export default CourierIntegrate;