import useTitle from '../../hooks/useTitle';

const ProductReport = () => {
    // Hook
    useTitle("Product Report");

    return (
        <>
            
        </>
    );
};

export default ProductReport;