import useTitle from '../../hooks/useTitle';

const OrderReport = () => {
    // Hook
    useTitle("Order Report");
    return (
        <>
            
        </>
    );
};

export default OrderReport;